# DiscoveryWorld failure-risk exploration, 2026-09-22 16:33 CST decision

The user requested exploration of whether the original graph -> neural failure risk -> bounded controller -> full-parameter ESOpt idea can work. Existing MADE negative outcomes and the V4 G0 activity halt remain immutable. This is development, not evidence of held-out improvement.

## Fixed next choice
Use a fixed posterior threshold of 0.5 under unit false-negative/unit false-positive risk-alarm costs. Do not search thresholds, cost ratios, seeds, or checkpoints for a positive result. Freeze the V4 DW bank, mathematical-reference qualification procedure, NN weights, temperature and preprocessing. This alarm rule is not a causal estimate of the value of revising a proposal.

Compare original Q75 and Posterior05 on the same G0 tensors and one predeclared fresh sampling seed world2/p304, B10, at most two proposals per step; reject if that identity already has a scientific claim. Shared public prompt, decoding and simulator, same actual graph qualification. Report all risk scores, revisions, rank changes, ActionSuccess failures, task score and inference costs. Share one model load and one actual eight-prefix qualification between the two development trajectories where exact unchanged G0 state permits. No test outcome is used for this decision.

CPU analysis may compare the old and fixed 0.5 rule on existing cal60/dev40/G010; report all partitions and no outcome-dependent follow-up threshold choice. Feature-family ablations use only the original world0 fit/world1 calibration/world2 development split, all four prespecified feature families, original NN recipe. They do not change the production predictor by selecting the best development AUROC.

## Complete-method continuation
The paired operating-point diagnostic alone is not the full method. A fresh explicit Full ES registration must retain the original actual full-parameter optimizer, original graph and NN support checks, current-state qualification, G0 controller-activity evidence, fixed independent evaluator, and development selection. Reuse an exactly matching accepted development episode only with explicit immutable provenance, never replay or count it as new. Parameter updates need tensor-hash/checkpoint evidence; zero update or selection of G0 is a legitimate null outcome.

An extra-proposal mechanism control is required before attributing downstream gains specifically to internal risk. The exact control and fixed held-out scope will be declared before their calls. Do not weaken Native or hide negative systems. MADE is terminal at 1079/1080 accepted plus one preserved technical failure. No new MADE tuning, benchmarks, or seed expansion is part of this exploration.
