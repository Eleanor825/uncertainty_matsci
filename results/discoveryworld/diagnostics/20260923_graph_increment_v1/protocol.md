# Frozen graph increment: immediate CPU diagnostic

This post-hoc diagnostic reads the three existing world4/policy336/B30 original-graph follow-up jobs: G0_UQ, selected G1 Full, and selected G1 without NN. Each accepted closed episode is a separate cohort; never pool G0/G1 or episodes. Anything not closed at the initial snapshot is reported unavailable without waiting. No-NN trajectories with no graph remain in coverage accounting rather than receiving fabricated features.

Use only the committed 2026-09-22 four-family ablation's frozen Internal293, NoGraph140 and GraphOnly153 next_action_failure models: original200 updates and each original calibration temperature. Check protocol, checkpoint SHA, tensor hash, feature names, training/calibration roles and Internal production-model reproduction. No fitting, step/threshold/checkpoint selection, policy modification, LLM/graph/environment invocation, or counterfactual labels. Record source bytes and raw-input hashes in one tiny Git protocol before the first CPU prediction.

Primary paired comparison is Internal minus NoGraph Brier and logloss; secondary comparisons include GraphOnly. Report AUROC when both classes exist; fixed logloss probability clipping is1e-7. Primary losses use only officially executed, labelled actions with the same original successful parsed/full-graph support for all families. Always report all requests, official actions, parser rejections, unsupported counts/reasons, and feature-None counts alongside conditional metrics. Do not treat supported-only scores as overall effectiveness, or claim NoGraph's natural capture coverage can be recovered from failed full-graph logs.

Join exact selected candidate and policy stamp to original official action/response/tick journals. Only that actually dispatched candidate gets its official failure label; parser rejections are separate. All generated supported candidates may receive frozen risks and shadow two-candidate rankings, but unexecuted outcomes stay empty. Compare Internal recalculation with previously recorded Internal probability to tolerance2e-6. The generated candidate set and executed outcomes were chosen by the original controller, so prediction/ranking differences do not estimate causal online policy improvement.

Write candidate-level risks/labels CSV, ranking CSV, per-cohort metrics/coverage and a short report in this new diagnostic namespace. Each cohort has one episode, with serially correlated actions, all in a single world/seed. No row-level confidence interval, significance or broad-generalization claim. Historical world2 metrics are context only. CPU wall cap600s; zero new fits/optimizer updates, graph captures, LLM or environment calls. Preserve original inputs and any incomplete diagnostic attempt.
