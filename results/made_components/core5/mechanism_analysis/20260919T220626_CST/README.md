# Complete MADE core60 mechanism analysis

This is a read-only analysis of the existing five systems × three evaluation seeds × four arms (B10): 60 trajectories,1,237 proposals,1,174 executed policy tools and600 candidate ORB steps. These are the same60 runs already published in the core5 result snapshot, not new experiments. The held-out NN/controller observations are descriptive, not causal evidence or a basis for test tuning. Read [REPORT.md](REPORT.md).

Recompute with **Python3.12** (stdlib only):

```bash
python3.12 -B analyze.py
```

The original analysis/source and original manifest remain under `sources/original_analyze.py` and `original_package_manifest.json`. Only input location, summary input reference and publication metadata changed in the executable copy; all mathematical statements and all original CSVs are byte-identical. The source projection `inputs/trajectories.jsonl.gz` is the exact original11.30MB gzip scientific projection (88.37MB decompressed), SHA6430dde5138feb880868d63744522e475bc8f18f3d7e59cce118e4c830519a05. It contains no login, host/process/operator records, password or browser material. The original projection summary is unchanged.

Python3.9.6 gives22 last-bit variance differences (maximum8.89e-16) and fails the original strict byte check; Python3.12.13 matches all original outputs exactly. This is not handled by changing stored results or loosening the verifier. The additional initial-G0-pairing/source-contract report artifacts are source-hashed supporting analysis, not separately regenerated by analyze.py. Absolute local input locations have been replaced by a portable relative reference; original and export hashes are distinguished in publication.json.

Pending/running jobs from the1080-run main matrix are absent from this fixed completed cohort and never enter its means. This package neither repeats science nor claims full-study completion.
