# Portable SnAr sampling+hidden diagnostic archive

Three additional132-feature CPU risk NNs completed180 optimizer steps with their own original prepare/run/audit rc0. Compare them with the existing all285 models on the same224 saved selected actions; no old model was refit. Read [REPORT.md](REPORT.md).

`python3 -B recompute.py` independently verifies the8 original JSON artifacts,672 new predictions,150 development prediction rows,policy-seed strata,420 fixed calibration bins and matched original-all comparisons. It requires only Python standard library. `--write` regenerates local derived tables, without inference or fitting. Original strict saved-model report acceptance is preserved separately from independent floating metric checks.

The archive includes exact scientific runner/plan/source-manifest bytes and references to inherited dependencies. Copied fit code has historical machine paths and is provenance, not a portable executable experiment. `.pt` checkpoints and API/auth/operator transport are excluded. Metric recomputation is portable; checkpoint refitting/reconstruction still needs the original scientific inputs/runtime and explicit fresh execution bindings.

Total NN work is746 original +180 new =926 optimizer steps across15 models. The combined3,360 predictions are still on224 unique physical actions, not3,360 independent experiments. Matched NN seeds and unequal parameter counts do not give a fixed-weight causal graph intervention. No controller is deployed. Published as a separately reviewed additive diagnostic; original physical results are unchanged.

The copied source manifest retains its historical pre-deployment status; actual completion/audit evidence is stored separately and is not overwritten by that source-freeze label.
