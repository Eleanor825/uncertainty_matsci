# Twenty-three pre-execution initialization failures: retained provenance

The exact23-job set had one logical initialization request per job and **zero physical oracle, candidate, tool or step calls**. All23 failed at the missing ORB asset guard before the actual CORE adapter constructed the oracle. The source/RPC/closure audit passed every recorded predicate. This is not an authorization to retry other failures or unfavorable scientific outcomes.

The set contains baseline4, independent ES-only5, UQ8 and full6 jobs;13 B10 and10 B30 jobs. Their **430 original candidate calls** remained to be performed at recovery. The administrative operation archived all23 original failure directories byte-for-byte and left the unchanged original atomic reservation mechanism to acquire new attempts. At the actual apply observation there were **zero new claims and zero new receipts**. No completed experiment, model, seed, evaluator or budget was changed; model-loading/waiting overhead is not erased.

`original_attempts.csv` retains all original job/attempt IDs. `scientific_provenance.json` retains exact original failure/claim/RPC and closed-phase SHA references, all audit predicates and the authoritative recovery receipt projection. Node names, process IDs, terminal/API material and executable resource operations are excluded. This package cannot perform a recovery.

The authoritative batch receipt SHA is `0878ce48531ff9b482e5c45e2e24e28f0c63efab344459f57e7309d96b3c5dfa`. An earlier screenshot transcription was corrected from the original file readback; the incorrect transcription remains explicitly marked as superseded in provenance. No source, plan or scientific bytes changed in that correction.

The initial conservative classifier checked an unused project adapter path. After verifying `CoreFinalRunner.project` and the actual environment cwd/PYTHONPATH, the source gate was corrected to the true CORE adapter. Its asset guard precedes output creation and every physical initialization call. Original conservative audit and failure history remain retained privately. The corrected source-selection and input hashes are recorded here.

This report is a recovery-time record, not a claim that the430 evaluations have finished. Later accepted results must retain their same registered job identity and link to the explicit archived attempt. Historical snapshots still show the original failures. `python3 -B verify.py` checks this portable evidence's counts, invariants and hashes; it does not rerun the raw RPC/liveness audit, mutate a claim or execute science.
